import { memberProfile } from 'data/member';

const TeamOverviewTab = (_props: (typeof memberProfile)['details']['teamOverview']) => {
  return <div>TeamOverviewTab</div>;
};

export default TeamOverviewTab;
